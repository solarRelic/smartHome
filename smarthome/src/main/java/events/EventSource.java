package events;

/**
 * An interface representing the source of an event in the system.
 * Classes implementing this interface act as sources that can generate events.
 */
public interface EventSource {
}
